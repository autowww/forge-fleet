services:
  fleet:
    build: .
    ports:
      - "18765:18765"
    environment:
      FLEET_BEARER_TOKEN: ${FLEET_BEARER_TOKEN:-}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: ["python3", "-m", "fleet_server", "--host", "0.0.0.0", "--port", "18765"]
