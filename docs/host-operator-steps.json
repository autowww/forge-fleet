[
  {
    "version": "0.3.54",
    "title": "Certificator template via API upload (no builtin Dockerfile in forge-fleet package)",
    "commands": [
      "# Forge Fleet no longer seeds certificator_source_ingest_worker from git. From forge-certificators:",
      "#   ./scripts/package-fleet-certificator-template.sh",
      "#   curl -T certificator_source_ingest_worker.tar.gz -H \"Authorization: Bearer $FORGE_FLEET_BEARER_TOKEN\" \\",
      "#     \"${FORGE_FLEET_BASE_URL}/v1/container-templates/certificator_source_ingest_worker/package?replace=1\"",
      "#   curl -X POST \"${FORGE_FLEET_BASE_URL}/v1/container-templates/build\" -H \"Authorization: Bearer $FORGE_FLEET_BEARER_TOKEN\" \\",
      "#     -H \"Content-Type: application/json\" -d '{\"requirement_ids\":[\"certificator_source_ingest_worker\"]}'",
      "# See forge-certificators/fleet-container-template/README.md and forge-fleet/docs/CONTAINER-TEMPLATES.md"
    ]
  },
  {
    "version": "0.3.49",
    "title": "Docker Engine with BuildKit buildx (requirement template builds)",
    "commands": [
      "# Full steps: docs/HOST-BOOTSTRAP.md",
      "# On Ubuntu (Docker apt repo configured):",
      "#   sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin",
      "#   sudo systemctl enable --now docker",
      "# Alternative without buildx: set FLEET_DOCKER_BUILDKIT=0 in forge-fleet.env (see systemd/environment.example)"
    ]
  }
]
